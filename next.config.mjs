/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Vendor logos are loaded from external hosts as plain <img>; no next/image optimization needed.
};

export default nextConfig;
